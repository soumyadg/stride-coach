# Cloudflare Pages headers — never cache the service worker or HTML entry points,
# so a new deploy reaches returning users immediately (no stale PWA shell).
/sw.js
  Cache-Control: no-cache, no-store, must-revalidate
/manifest.webmanifest
  Cache-Control: no-cache
/*.html
  Cache-Control: no-cache, must-revalidate
